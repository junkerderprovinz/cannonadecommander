Menu="VMs"
Icon="rocket"
Tag="rocket"
---
<?php
/* CannonadeCommander VM-tab injector. TITLE-LESS on purpose: Unraid's VMs page is
   Tabs="true", and a title-less child goes through the tabbed layout's inline-eval
   branch (Unraid 7.1+, the 2025 tab refactor), so it renders NO visible tab or panel of
   its own — it just loads vms.js, which tints the native VM row icons on the "Virtual
   Machines" tab. (A titled page would instead show up as a separate "CannonadeCommander"
   tab, which is only needed to load on pre-7.1 builds that don't have the inline-eval
   branch; there VM-icon tinting simply won't apply.) */
?>
<script defer src="<?autov('/plugins/cannonadecommander/scripts/vms.js')?>"></script>
